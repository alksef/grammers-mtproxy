**/target
**/*.rs.bk
Cargo.lock
*.session

# examples/ file to test changes, might contain sensitive information
testbed.rs

# scraped docs
*.json

# FakeTLS wire dumps (contain client traffic metadata — not for public commit)
docs/faketls/dumps/
